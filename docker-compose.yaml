version: "3.4"

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - .:/app
    command: sh -c "composer install && php-fpm"
    healthcheck:
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 30s